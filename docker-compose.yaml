services:
  web:
    build: .
    container_name: jtech_web
    ports:
      - "8000:80"
    volumes:
      - ./src:/var/www/html
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: mysql:8.0
    container_name: jtech_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: jtech_db
    volumes:
      - ./database/jtech_db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "3306:3306"